# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/mindpulse?retryWrites=true&w=majority

# Server port
PORT=5000

# JWT secret for future auth features
JWT_SECRET=mindpulse_secret_key_2026

# Node environment (development / production)
NODE_ENV=development
